#include <stdlib.h>
#include <stdio.h>
/**
 * main - print numbers without char type
 *
 * Return: 0
*/

int main(void)
{
	int ch;

	for (ch = '0'; ch <= '9'; ch++)
	{
		putchar(ch);
	}

	putchar('\n');

return (0);
}
